<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug-only. Android 9+ blocks cleartext HTTP by default, and 10.0.2.2 (the
  emulator alias for the host machine) is not treated as localhost. Release
  builds do not include this file, so production traffic stays HTTPS-only.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
